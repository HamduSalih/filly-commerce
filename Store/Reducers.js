import { combineReducers } from "redux";

// import { LoginReducer as login } from "../Screens/Login/Modules";

export const makeRootReducer = () => {
  return combineReducers({
  });
};

export default makeRootReducer;
